package ext

import (
	"testing"
)

func TestPCall(t *testing.T) {
	//PCall(func() {
	//	a := 0
	//	b := 1000 / a
	//	_ = b
	//})

	// a, b, c := PCall(func(a string, b int, c bool) (string, int, bool) {
	// 	return a, b, c
	// }, "s", -1, true)
}
